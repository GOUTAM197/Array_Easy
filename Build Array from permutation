class Solution {
public:
     vector<int> buildArray(vector<int> num) {
		vector<int> ans;
           int n = num.size();
           for(int i = 0; i < n; i++){
                ans.push_back(num[num[i]]);

           }
           return ans;
     }
};
